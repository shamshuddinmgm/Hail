<?xml version="1.0" encoding="utf-8"?>
<!-- Solid colors only — theme attrs inside ColorStateList crash inflate on some OEMs (HyperOS). -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:color="@color/color_hail" android:state_checked="true" />
    <item android:color="@color/color_hail" android:state_selected="true" />
    <item android:color="#8A9099" />
</selector>
